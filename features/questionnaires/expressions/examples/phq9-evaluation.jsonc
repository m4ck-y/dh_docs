{
  /* evaluation_expression del PHQ-9: clasificacion en 5 bandas.
     Raiz: case/when. El subject CONSUME form.scoring_result (no repite la
     formula del scoring). Ver ../README.md §1 y ../operators/case.md.
     Bandas por umbral acumulativo (primera coincidencia gana). */
  "expression": {
    "type": "case",
    "operator": "when",
    "subject": {
      "subject": { "entity": "form", "property": "scoring_result" }
    },
    "cases": [
      {
        "when": {
          "operator": "<",
          "operand": { "const": { "value": 5, "type": "number" } }
        },
        "then": { "const": { "value": "Depresión mínima", "type": "string" } }
      },
      {
        "when": {
          "operator": "<",
          "operand": { "const": { "value": 10, "type": "number" } }
        },
        "then": { "const": { "value": "Depresión leve", "type": "string" } }
      },
      {
        "when": {
          "operator": "<",
          "operand": { "const": { "value": 15, "type": "number" } }
        },
        "then": { "const": { "value": "Depresión moderada", "type": "string" } }
      },
      {
        "when": {
          "operator": "<",
          "operand": { "const": { "value": 20, "type": "number" } }
        },
        "then": { "const": { "value": "Depresión moderadamente severa", "type": "string" } }
      },
      {
        "when": {
          "operator": "<=",
          "operand": { "const": { "value": 27, "type": "number" } }
        },
        "then": { "const": { "value": "Depresión severa", "type": "string" } }
      }
    ],
    "default": { "const": { "value": "Puntuación fuera de rango", "type": "string" } },
    "output": { "type": "string" },
    "args": []
  }
}
