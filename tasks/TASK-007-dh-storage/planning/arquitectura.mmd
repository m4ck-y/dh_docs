# Arquitectura: dh_storage

```mermaid
flowchart LR
    subgraph dh_storage["dh_storage"]
        direction TB
        API["API Layer"]
        UC["Use Cases"]
        SRV["Storage Abstraction"]
        
        API --> UC
        UC --> SRV
    end
    
    subgraph storage_backends["Storage Backends"]
        direction LR
        DISK["Disk Storage"]
        GCS["GCS Storage"]
    end
    
    subgraph postgres["PostgreSQL - storage"]
        direction LR
        DCAT["document_category"]
        DTYPE["document_type"]
        DOC["document"]
    end
    
    subgraph mongo["MongoDB"]
        DTC["document_type_config"]
    end
    
    dh_onboarding --> dh_storage
    dh_storage --> DISK
    dh_storage --> GCS
    dh_storage --- postgres
    dh_storage --- mongo
    
    DCAT --> DTYPE
    DTYPE --> DOC
```

## Enums

```mermaid
erDiagram
    EStorageProvider {
        String DISK "disk"
        String GCS "gcs"
    }
```

## Flujo de Operativas

| Operación | Storage | DB |
|-----------|---------|-----|
| Upload | EStorageProvider | PostgreSQL `document` |
| Download | EStorageProvider | PostgreSQL `document` |
| Review | - | PostgreSQL `document` |
| Validation Config | - | MongoDB |