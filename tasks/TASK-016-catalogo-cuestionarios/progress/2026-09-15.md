# Progreso — 2026-09-15

## Resumen

Definición del modelo del catálogo y organización del feature de cuestionarios.

## Commits agrupados

| Commit | Tema |
|---|---|
| `28e6868` | `config` definitivo por tipo de pregunta (C5) |
| `f7f9f25` | `answer.value` canónico como `{value, data_type}` (C9) |
| `ae60f65` | renombre de campos de tipo en el AST y `answer` |
| `36b8e40` | agrupar expresiones en envelopes `expression`/`result` |
| `61d259e` | PHQ-9 ítem 7 (C6) |
| `12c5658` | IPAQ METs scoring (C7b) |
| `83c5c5a` | limpieza de ruido E13 |
| `002899d` | refresh de TODO + nota de excepción C6 |
| `cccd35a` | propiedades derivadas del AST (ADR 040) |
| `04b1e40` | refresh C7c en TODO |
| `c3c7118` | cierre C7c (`question.expression` + `answer.source`) |
| `f21485c` | crear banco del catálogo + mover contrato del motor a `reference/` |
| `e473280` | agregar `wellbeing_nutritional` a categorías del banco |

## Decisiones

- Banco centralizado en `features/questionnaires/catalog/bank/`
  (`instruments/` + `clinical_history/` + `categories.json`).
- Contrato del motor frontend movido a `features/questionnaires/reference/`.
- Pendientes trasladados desde `docs/TODO/cuestionarios.md` a
  `planning/pendientes.md`.

## Siguientes pasos

- Generar los JSON de instrumentos del banco.
- Resolver `EBiologicalSex` y `reference.type_media` (`schema.sql`).
