# Progreso — 2026-09-15

## Resumen

Consolidación de la historia clínica como feature propio y de sus diagramas de
AHF.

## Commits agrupados

| Commit | Tema |
|---|---|
| `55e9efe` | eliminar `historial_clinico/diagram/` y corregir referencias |
| `026e62b` | separar `docs/diagrams/` por formato (source/flows/reviews/activation) |
| `a167b48` | consolidar el árbol de AHF en un único `family_tree.html` |
| `a4c688b` | fusionar el detalle de AHF en el `.mmd` canónico y quitar drawio duplicado |
| `325c31d` | migrar los docs de AHF a `features/clinical_history` |

## Decisiones

- Historia clínica = módulo feature Nº 2 (depende de `questionnaires`).
- Los prototipos HTML viven en `proposals/family_condition/`.
- El mapeo a dominio se centraliza en el feature `mapper` (Nº 3).

## Sesión (análisis)

- Revisadas las 5 secciones (`docs/diagrams/0_HISTORIA_CLINICA/flows/*.mmd`) y las
  propuestas de AHF (`features/clinical_history/proposals/family_condition/`).
- Identificados stoppers → registrados en `planning/OPEN-QUESTIONS.md`
  (H1 AHF catálogo vs form; H2 grupos repetibles; H3 catálogos externos; H4 anexos).
- Confirmado: **AHF es una matriz** familiar × enfermedad (no un grupo repetible);
  **D** sí requiere repetición (cirugías/lesiones/hospitalizaciones).

## Siguientes pasos

- Redactar fichas de estructura **C (APNP)** y **E (Padecimiento actual)** — sin bloqueos.
- Resolver **H1** (AHF) y **H2** (repetibles) antes de modelar **B** y **D**.
- Generar el **JSON de HC** en `bank/clinical_history/` tras cerrar B/D.
