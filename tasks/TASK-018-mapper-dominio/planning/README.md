# planning — TASK-018

- Feature: [`features/mapper/`](../../../features/mapper/README.md).
- Contrato (borrador): `features/mapper/README.md` y
  `features/mapper/examples/binding.example.jsonc`.
- Vistas por formulario: `features/mapper/views/<dominio>/`.
- Depende de `questionnaires` y `clinical_history`.
