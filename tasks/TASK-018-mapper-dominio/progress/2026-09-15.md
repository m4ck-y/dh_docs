# Progreso — 2026-09-15

## Resumen

Registro del mapper como feature transversal (Nº 3) y centralización del mapeo
de dominio.

## Commits agrupados

| Commit | Tema |
|---|---|
| `d48cd8f` | crear el feature `mapper` y centralizar el mapeo de dominio |

## Decisiones

- Feature transversal (depende de `questionnaires` + `clinical_history`).
- Contrato de binding: `read` (prefill) / `write` (write-through) / `transform` /
  `conflict` / `enabled`.
- El mapeo de las secciones se centraliza en `mapper/views/<dominio>/`.

> **Nota (posterior):** revertido. El mapper es **solo preguntas** de `form`; el
> contrato de los **componentes** (A/B/D) **no** vive en `mapper/views/`
> (eliminado). Ver [`../../features/mapper/README.md`](../../../features/mapper/README.md).

## Siguientes pasos

- Definir el store en Mongo y la UI administrativa.
